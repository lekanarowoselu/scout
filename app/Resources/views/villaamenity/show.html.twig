{% extends 'base.html.twig' %}

{% block body %}
    <h1>VillaAmenity</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ villaAmenity.id }}</td>
            </tr>
            <tr>
                <th>Metatitle</th>
                <td>{{ villaAmenity.metaTitle }}</td>
            </tr>
            <tr>
                <th>Metadescription</th>
                <td>{{ villaAmenity.metaDescription }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ villaAmenity.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ villaAmenity.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('villaamenity_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('villaamenity_edit', { 'id': villaAmenity.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
