{% extends 'base.html.twig' %}

{% block body %}
    <h1>VillaService</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ villaService.id }}</td>
            </tr>
            <tr>
                <th>Metatitle</th>
                <td>{{ villaService.metaTitle }}</td>
            </tr>
            <tr>
                <th>Metadescription</th>
                <td>{{ villaService.metaDescription }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ villaService.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ villaService.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('villaservice_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('villaservice_edit', { 'id': villaService.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
